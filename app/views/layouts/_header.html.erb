<div class="d-flex flex-column flex-md-row align-items-right p-3 px-md-4 mb-3 bg-white border-bottom">
<% if user_signed_in? %>
    <span>Здравствуйте, <%= current_user.email %></span>
    <%= link_to 'Выйти', destroy_user_session_path, :method => :delete %>
<% else %>
    <%= link_to 'Войти', new_user_session_path %>&nbspили&nbsp<%= link_to 'Зарегистрироваться', new_user_registration_path %>
<% end %></div>
<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
<h5 class="my-0 mr-md-auto font-weight-bold" style="color: gray">Расписание занятий</h5>
<nav class="my-2 my-md-0 mr-md-2">
<span class="p-2 text-dark"></span>

<a href="<%= dayweeks_path %>" class="btn btn-sm btn-info" role="button">Расписание</a>
<a href="<%= lessons_path %>" class="btn btn-sm btn-info" role="button">Предметы</a>
<a href="<%= shedulers_path %>" class="btn  btn-sm btn-success" role="button">Расписание на неделю</a>
<% if user_signed_in? %>
    <a href="<%= new_dayweek_path %>" class="btn btn-sm btn-danger " role="button">Новый день</a>
    <a href="<%= new_lesson_path %>" class="btn btn-sm btn-danger" role="button">Новый предмет</a>
    <a href="<%= new_sheduler_path %>" class="btn btn-sm btn-danger" role="button">Новое расписание</a>
<% end %>    
</div>
</nav>
<br>
